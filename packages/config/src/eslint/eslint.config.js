// apps/web/eslint.config.js
import baseConfig from '@monorepo/eslint-config'

export default [
  ...baseConfig,
  {
    // Règles spécifiques à l'application web si nécessaire
    rules: {
      // Vos règles personnalisées
    }
  }
]
